<%= form_for(@subgrupo) do |f| %>
  <% if @subgrupo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subgrupo.errors.count, "error") %> prohibited this subgrupo from being saved:</h2>

      <ul>
      <% @subgrupo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
