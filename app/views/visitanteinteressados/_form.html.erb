<%= form_for(@visitanteinteressado) do |f| %>
  <% if @visitanteinteressado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visitanteinteressado.errors.count, "error") %> prohibited this visitanteinteressado from being saved:</h2>

      <ul>
      <% @visitanteinteressado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
