
<div class="container">

<%= form_for(@marca, html: { class: 'form-horizontal' }) do |f| %>
 <div class="row">
  <% if @marca.errors.any? %>
    <div class="alert alert-error">
      <h3><%= pluralize(@marca.errors.count, "error") %> prohibited this job from being saved:</h3>

      <ul>
      <% @marca.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :descricao, class: 'control-label' %>
    <div class="controls">
      <%= f.text_field :descricao%>
    </div>
  </div>
    <div class="control-group controls">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
  
<% end %>
</div>
</div>